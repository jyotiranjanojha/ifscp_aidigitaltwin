"""sp_digitaltwin smoke test package.

Usage:
    python -m smoke_tests.runner              # All tests
    python -m smoke_tests.runner --backend    # Backend only
    python -m smoke_tests.runner --frontend   # Frontend only
    python -m smoke_tests.runner --live       # Live integration only
    python -m smoke_tests.runner --quick      # Skip slow frontend checks
"""
